def mod_name = "XPTeleporters";
def mod_group = "latmod.xpt";
def mod_class = "XPT.java";
def mod_version = "1.0.0";

buildscript {
    repositories {
        mavenCentral()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
        maven {
            name = "sonatype"
            url = "https://oss.sonatype.org/content/repositories/snapshots/"
        }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:1.2-SNAPSHOT'
    }
}

apply plugin: 'forge'

ext.configFile = file "build.props"

configFile.withReader {
    def prop = new Properties()
    prop.load(it)
    project.ext.config = new ConfigSlurper().parse prop
}

version = config.version_mc + "-" + mod_version
group = mod_group
archivesBaseName = mod_name

minecraft {
    version = config.version_mc + "-" + config.version_forge
    runDir = "eclipse/assets"
    
    replace "@VERSION@", mod_version
    replaceIn mod_class
}

dependencies {
}

sourceSets {
    main {
        java {
            srcDir config.source_dir + mod_name + "/src/main/java"
        }
        resources {
            srcDir config.source_dir + mod_name + "/src/main/resources"
        }
    }
}

tasks.withType(Jar)
{
    destinationDir = file("$rootDir/sources/Exported")
}

processResources
{
    // this will ensure that this task is redone when the versions change.
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    // replace stuff in mcmod.info, nothing else
    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
                
        // replace version and mcversion
        expand 'version':project.version, 'mcversion':project.minecraft.version
    }
        
    // copy everything else, thats not the mcmod.info
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}